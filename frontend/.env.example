# For local dev, you can leave this unset — axios.js falls back to
# http://localhost:8000/api automatically.
#
# For production (Vercel/Netlify), set this to your deployed backend's
# URL, e.g.:
VITE_API_BASE_URL=https://your-backend.onrender.com/api